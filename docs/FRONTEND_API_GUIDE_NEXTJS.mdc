# Frontend API Guide (Next.js + fetch) - comvia

> Bắt đầu tại [`docs/README.md`](./README.md). File này là tài liệu API chính cho app Next.js (user/workspace/staff).

Tài liệu này hướng dẫn FE Next.js gọi backend NestJS theo các API hiện có trong dự án `e-Nest-comvia`.

## 1) Base URL & Environments

### 1.1 Biến môi trường gợi ý
- `NEXT_PUBLIC_API_BASE_URL`: **gốc API đầy đủ, gồm cả global prefix** `api/v1` (backend gọi `app.setGlobalPrefix('api/v1')` trong `main.ts`).
- `NEXT_PUBLIC_DEMO_MODE` (tùy bạn; backend đọc `DEMO_MODE` ở server)

Ví dụ `.env.local` (Next.js):
```env
# Local Nest (port mặc định 3000)
NEXT_PUBLIC_API_BASE_URL="http://localhost:3000/api/v1"

# Deploy (ví dụ server của bạn — đổi host/port nếu khác)
NEXT_PUBLIC_API_BASE_URL="http://103.200.20.197:3000/api/v1"
```

Các đoạn `comviaFetch("/auth/login", …)` bên dưới nối `path` vào **sau** base URL trên (tức thành `…/api/v1/auth/login`).

## 2) Chuẩn hóa cách gọi API bằng fetch

### 2.1 Helper `comviaFetch`
Bạn có thể copy trực tiếp theo kiểu TS:
```ts
// app/lib/comviaFetch.ts
export type ComviaApiError = {
  statusCode?: number;
  message?: string | string[];
  error?: string;
};

function readJsonSafe<T>(res: Response): Promise<T | null> {
  const ct = res.headers.get("content-type") ?? "";
  if (!ct.includes("application/json")) return Promise.resolve(null);
  return res.json().catch(() => null);
}

export async function comviaFetch<T>(
  path: string,
  init?: RequestInit & { apiKey?: string; token?: string },
): Promise<T> {
  const baseUrl = process.env.NEXT_PUBLIC_API_BASE_URL;
  if (!baseUrl) throw new Error("Missing NEXT_PUBLIC_API_BASE_URL");

  const url = `${baseUrl}${path}`;

  const headers = new Headers(init?.headers);
  headers.set("Content-Type", "application/json");

  if (init?.token) headers.set("Authorization", `Bearer ${init.token}`);
  if (init?.apiKey) {
    // backend Public API có thể đọc x-api-key (hoặc body/query api_key)
    headers.set("x-api-key", init.apiKey);
  }

  const res = await fetch(url, {
    ...init,
    headers,
  });

  if (!res.ok) {
    const data = await readJsonSafe<ComviaApiError>(res);
    const msg =
      (typeof data?.message === "string" && data.message) ||
      (Array.isArray(data?.message) && data.message.join(", ")) ||
      data?.error ||
      `HTTP ${res.status}`;

    throw new Error(msg);
  }

  return (await res.json()) as T;
}
```

### 2.2 Format body chung
- Backend NestJS đang dùng `ValidationPipe` nên body phải đúng field/type theo DTO.
- Tất cả ví dụ dưới đây dùng JSON `Content-Type: application/json`.

## 3) JWT Auth (Login / Me / OTP)

### 3.1 Register (OTP demo)

Endpoint:
- `POST /auth/register` (Public)

Request:
```json
{
  "email": "demo.user@example.com",
  "password": "12345678",
  "fullName": "Demo User"
}
```

Response (khi `DEMO_MODE=true`):
```json
{
  "id": "USER_ID",
  "email": "demo.user@example.com",
  "fullName": "Demo User",
  "role": "USER",
  "createdAt": "2026-03-20T07:..Z",
  "otpRequestId": "OTP_REQUEST_ID",
  "expiredAt": "2026-03-20T07:..Z",
  "demoOtpCode": "123456"
}
```

Ghi chú:
- Sau register, user ở trạng thái `PENDING_VERIFICATION`.
- Bạn phải gọi `otp/verify` trước khi `login` được.

FE call ví dụ:
```ts
const registerRes = await comviaFetch<any>("/auth/register", {
  method: "POST",
  body: JSON.stringify({ email, password, fullName }),
});
const otpCode = registerRes.demoOtpCode; // chỉ có khi DEMO_MODE=true
```

### 3.2 Verify OTP (đổi sang ACTIVE + nhận accessToken)

Endpoint:
- `POST /auth/otp/verify` (Public)

Request:
```json
{
  "targetType": "EMAIL",
  "targetValue": "demo.user@example.com",
  "purpose": "REGISTER",
  "otpCode": "123456"
}
```

Response:
```json
{
  "accessToken": "JWT_ACCESS_TOKEN",
  "tokenType": "Bearer",
  "user": {
    "id": "USER_ID",
    "email": "demo.user@example.com",
    "fullName": "Demo User",
    "role": "USER",
    "status": "ACTIVE"
  }
}
```

### 3.3 Login

Endpoint:
- `POST /auth/login` (Public)

Request:
```json
{
  "email": "demo.user@example.com",
  "password": "12345678"
}
```

Response:
```json
{
  "accessToken": "JWT_ACCESS_TOKEN",
  "tokenType": "Bearer",
  "user": {
    "id": "USER_ID",
    "email": "demo.user@example.com",
    "fullName": "Demo User",
    "role": "USER"
  }
}
```

### 3.4 Me

Endpoint:
- `GET /auth/me`

Headers:
- `Authorization: Bearer <accessToken>`

Response:
```json
{
  "id": "USER_ID",
  "email": "demo.user@example.com",
  "fullName": "Demo User",
  "role": "USER",
  "status": "ACTIVE",
  "createdAt": "2026-03-20T07:..Z"
}
```

### 3.5 Update my profile

Endpoint:
- `PATCH /users/me`

Headers:
- `Authorization: Bearer <accessToken>`

Request:
```json
{
  "fullName": "Demo User Updated"
}
```

## 4) Workspace APIs (cốt lõi cho mọi nghiệp vụ)

Tất cả route workspace hiện đang theo pattern:
- `/workspaces/:workspaceId/...`

### 4.1 Create workspace (OWNER)
Endpoint:
- `POST /workspaces`

Headers:
- JWT

Request:
```json
{
  "name": "Demo Workspace",
  "slug": "demo-workspace",
  "billing": {
    "billingType": "ORGANIZATION",
    "companyName": "Demo Company",
    "taxCode": "0312345678",
    "address": "123 Demo Street",
    "invoiceEmail": "billing@demo.com",
    "representativeName": "Nguyen Van A",
    "phone": "0901234567"
  }
}
```

Hoặc loại `INDIVIDUAL`:
```json
{
  "name": "Demo Workspace",
  "slug": "demo-workspace",
  "billing": {
    "billingType": "INDIVIDUAL",
    "fullName": "Nguyen Van B",
    "citizenId": "079123456789",
    "taxCode": "8561234567",
    "address": "123 Demo Street",
    "invoiceEmail": "demo.user@example.com",
    "phone": "0901234567"
  }
}
```

Response (tối giản):
```json
{
  "id": "WS_ID",
  "name": "Demo Workspace",
  "slug": "demo-workspace",
  "ownerUserId": "USER_ID",
  "status": "ACTIVE",
  "createdAt": "...",
  "billingProfile": {
    "billingType": "ORGANIZATION",
    "companyName": "Demo Company",
    "taxCode": "0312345678",
    "address": "123 Demo Street",
    "invoiceEmail": "billing@demo.com"
  }
}
```

### 4.2 List my workspaces
Endpoint:
- `GET /workspaces`

Response:
```json
[
  {
    "id": "WS_ID",
    "name": "Demo Workspace",
    "slug": "demo-workspace",
    "status": "ACTIVE",
    "role": "OWNER",
    "joinedAt": "..."
  }
]
```

### 4.3 Switch workspace
Endpoint:
- `POST /workspaces/:workspaceId/switch`

Response:
```json
{ "id": "USER_ID", "lastActiveWorkspaceId": "WS_ID" }
```

## 5) Members (Owner mời, user accept)

### 5.1 List members
- `GET /workspaces/:workspaceId/members`

### 5.2 Invite member
- `POST /workspaces/:workspaceId/invitations`

Auth/Role: `OWNER`

Request:
```json
{
  "inviteType": "EMAIL",
  "inviteValue": "member@example.com",
  "role": "MEMBER"
}
```

Response:
```json
{
  "id": "INVITATION_ID",
  "token": "INVITE_TOKEN",
  "expiredAt": "..."
}
```

### 5.3 Accept invitation
- `POST /workspaces/:workspaceId/invitations/accept`

Auth: user đăng nhập (JWT)

Request:
```json
{
  "token": "INVITE_TOKEN"
}
```

Response:
```json
{ "ok": true }
```

Ghi chú audit log:
- Backend tự ghi `member.invited` / `member.removed` khi invite / xóa member.

## 6) OA Connection (Zalo OAuth + ZNS)

Backend tích hợp **Zalo Official Account (OA)** thật qua OAuth PKCE. Token được lưu trong DB theo workspace (không dùng Redis).

**Điều kiện trước Template/Messaging:** `oa.status === CONNECTED` và template đã `APPROVED` có `providerTemplateId`.

### 6.1 Endpoint workspace

Base:
- `/workspaces/:workspaceId/oa`

Headers (mọi endpoint dưới đây):
- `Authorization: Bearer <accessToken>`

Auth/Role:
- `GET status`: `OWNER` hoặc `MEMBER`
- `POST connect`, `POST disconnect`: `OWNER`

| Method | Path | Mô tả |
|--------|------|--------|
| `GET` | `/workspaces/:workspaceId/oa/status` | Xem trạng thái kết nối OA |
| `POST` | `/workspaces/:workspaceId/oa/connect` | Bắt đầu OAuth — trả URL cấp quyền Zalo |
| `POST` | `/workspaces/:workspaceId/oa/disconnect` | Ngắt kết nối, xóa token |
| `GET` | `/workspaces/:workspaceId/oa/connect/redirect` | Redirect HTTP 302 thẳng sang Zalo (cần JWT; SPA ưu tiên `POST connect`) |

### 6.2 Luồng kết nối OA (Zalo OAuth thật — không mock)

```mermaid
sequenceDiagram
  participant U as User (Owner)
  participant FE as Next.js App
  participant API as Comvia API
  participant Zalo as Zalo OAuth
  U->>FE: Mở /app/w/:wsId/oa → bấm Kết nối
  FE->>API: POST /workspaces/:wsId/oa/connect
  API-->>FE: authorizationUrl + state (gắn wsId)
  FE->>FE: sessionStorage workspaceId (fallback)
  FE->>Zalo: window.location = authorizationUrl
  Zalo->>API: GET /oa/auth/callback?code&state
  API->>Zalo: Đổi code + PKCE → lưu token
  API->>FE: 302 → /app/w/:wsId/oa?status=success
  FE->>API: GET /workspaces/:wsId/oa/status
  FE->>U: Hiển thị CONNECTED trên cùng trang OA
```

**Nguyên tắc UX:** User bắt đầu OAuth tại trang workspace OA (`/app/w/{workspaceId}/oa`) → sau cấp quyền Zalo phải **quay lại đúng URL đó** (cùng `workspaceId`), không để user “lạc” sang trang settings chung.

---

### 6.2.1 Khuyến nghị chốt (ưu tiên cho Backend implement)

| Ưu tiên | Ai | Việc cần làm |
|--------|-----|----------------|
| **A (khuyến nghị)** | **Backend** | Sau `GET /oa/auth/callback` thành công/thất bại → **HTTP 302** về URL workspace OA (xem §6.2.2). |
| **A** | **Backend** | `state` OAuth phải resolve được `workspaceId` (đã gửi lúc `POST connect`). |
| **B** | FE | Trước khi mở `authorizationUrl`: lưu `workspaceId` vào `sessionStorage` (fallback nếu backend chưa append query). |
| **B** | FE | Trang `/app/w/[workspaceId]/oa` đọc `?status=success\|error` → thông báo + `GET .../oa/status` → xóa query. |
| **C (fallback)** | Backend + FE | Chỉ dùng nếu chưa build redirect động: `ZALO_OAUTH_SUCCESS_REDIRECT_URL` → `/app/settings/oa` → FE chuyển tiếp sang `/app/w/{workspaceId}/oa` (§6.2.5). |

**Production (Comvia):** base app = `https://app.comvia.cloud`. Ví dụ user đang tại:

`https://app.comvia.cloud/app/w/cmoieuvb30001mlixf3krbauu/oa`

→ Backend redirect sau OAuth **nên là:**

`https://app.comvia.cloud/app/w/cmoieuvb30001mlixf3krbauu/oa?status=success`

(lỗi: `...?status=error&message=...` — `message` URL-encoded, optional).

---

### 6.2.2 Contract redirect sau callback (Backend — spec để implement)

**Input:** `GET /oa/auth/callback?code=...&state=...&oa_id=...` (public, không JWT).

**Backend phải:**

1. Parse `state` → lấy `workspaceId` (và `connectionId` nếu đã lưu lúc `POST connect`).
2. Đổi `code` + PKCE verifier (lưu theo `state`/`connectionId`) → access/refresh token Zalo.
3. Cập nhật `WorkspaceOaConnection` → `status = CONNECTED` (hoặc lỗi tương ứng).
4. Ghi audit `oa.connected` khi thành công.
5. **Redirect 302** về frontend — **không** trả JSON cho browser.

**Base URL frontend:** env `COMVIA_APP_BASE_URL` (hoặc tương đương), ví dụ `https://app.comvia.cloud`. **Không** hard-code path `/settings/oa` làm đích mặc định duy nhất trên production.

**URL đích khuyến nghị (build động từ `workspaceId` trong `state`):**

```
{COMVIA_APP_BASE_URL}/app/w/{workspaceId}/oa?status=success
{COMVIA_APP_BASE_URL}/app/w/{workspaceId}/oa?status=error&message={optional}
```

**Query params FE đọc (ổn định, snake/camel đều được FE hỗ trợ trên trang callback fallback):**

| Param | Bắt buộc | Giá trị | Ghi chú |
|-------|----------|---------|---------|
| `status` | Có | `success` \| `error` | FE hiển thị banner + refetch status |
| `workspaceId` | Khuyến nghị trên fallback | ID workspace | Bắt buộc nếu redirect về `/app/settings/oa`; trên URL workspace có thể bỏ (đã có trong path) |
| `message` | Không | string | Chỉ khi `status=error`, hiển thị lý do |
| `connected` | Không | `1` | Tương thích cũ; FE coi `status=success` là đủ |

**`state` khi tạo `POST /workspaces/:workspaceId/oa/connect` (gợi ý):**

- Encode opaque token **hoặc** JSON signed chứa tối thiểu: `workspaceId`, `connectionId`, `codeVerifier` (PKCE), `exp`.
- Sau callback: validate `state` (TTL, one-time) rồi mới redirect.

**Pseudo-code redirect (NestJS):**

```ts
const appBase = process.env.COMVIA_APP_BASE_URL ?? 'https://app.comvia.cloud';
const wsId = resolved.workspaceId; // từ state DB

if (ok) {
  return res.redirect(
    `${appBase}/app/w/${wsId}/oa?status=success`,
  );
}
return res.redirect(
  `${appBase}/app/w/${wsId}/oa?status=error&message=${encodeURIComponent(errMessage)}`,
);
```

**Không dùng mock:** `POST connect` phải trả `authorizationUrl` thật (`oauth.zaloapp.com/...`). Không set `CONNECTED` ngay trong `POST connect` mà chưa qua Zalo callback.

---

### 6.2.3 Các bước API chi tiết

**Bước 1 — Gọi connect (OWNER):**

```http
POST /workspaces/:workspaceId/oa/connect
Authorization: Bearer <accessToken>
```

Response:

```json
{
  "authorizationUrl": "https://oauth.zaloapp.com/v4/oa/permission?app_id=...&redirect_uri=...&code_challenge=...&state=...",
  "state": "opaque-or-signed-state",
  "connectionId": "OA_CONN_ID"
}
```

**Bước 2 — FE mở Zalo (đã implement):**

```ts
rememberOaOAuthWorkspace(workspaceId); // fallback sessionStorage
const { authorizationUrl } = await comviaFetch<OaConnectResponse>(
  `/workspaces/${workspaceId}/oa/connect`,
  { method: "POST", token: accessToken },
);
window.location.href = authorizationUrl;
```

**Bước 3 — Zalo → Backend callback (public):**

```http
GET /oa/auth/callback?code=<AUTH_CODE>&state=<STATE>&oa_id=<OA_ID?>
```

- `redirect_uri` đăng ký trên Zalo Developer Console phải trùng `ZALO_OAUTH_REDIRECT_URI` (API host), ví dụ `https://<api-host>/api/v1/oa/auth/callback`.
- Không nhầm với URL redirect về **app** (mục 6.2.2).

**Bước 4 — FE sau redirect (trang workspace OA):**

Route: `/app/w/[workspaceId]/oa` (`app/app/w/[workspaceId]/oa/page.tsx`).

- Đọc `status=success|error` (và optional `message`).
- Thông báo kết quả, gọi `GET /workspaces/:workspaceId/oa/status`, `router.replace` về URL sạch (không còn query).

---

### 6.2.4 Phương án fallback — `/app/settings/oa` (chỉ khi backend chưa redirect thẳng workspace)

Nếu tạm thời vẫn dùng env tĩnh:

```env
ZALO_OAUTH_SUCCESS_REDIRECT_URL=https://app.comvia.cloud/app/settings/oa
```

Backend redirect:

```
https://app.comvia.cloud/app/settings/oa?status=success&workspaceId={workspaceId}
```

FE (`/app/settings/oa`) chuyển tiếp → `/app/w/{workspaceId}/oa?oa_oauth=success`.

Alias tương thích doc cũ: `/settings/oa` → redirect sang `/app/settings/oa` (giữ query).

**Lưu ý:** Phương án này thêm một hop; production nên chuyển sang §6.2.2 khi có thể.

---

### 6.2.5 Biến môi trường Backend (checklist)

| Env | Mục đích | Ví dụ |
|-----|----------|-------|
| `ZALO_OAUTH_REDIRECT_URI` | Callback **API** (Zalo gọi) | `https://api.comvia.cloud/api/v1/oa/auth/callback` |
| `COMVIA_APP_BASE_URL` | Origin **frontend** để build URL sau OAuth | `https://app.comvia.cloud` |
| `ZALO_OAUTH_SUCCESS_REDIRECT_URL` | **Chỉ fallback** nếu không build URL động `/app/w/{id}/oa` | `https://app.comvia.cloud/app/settings/oa` |
| Zalo app credentials | `app_id`, secret, PKCE | Zalo Developer Console |

**Deprecated / tránh:** redirect mặc định duy nhất tới `https://app.comvia.cloud/settings/oa` (thiếu prefix `/app` và thiếu `workspaceId`) — FE production không dùng route đó làm trang đích cuối.

### 6.3 Get OA status

```http
GET /workspaces/:workspaceId/oa/status
Authorization: Bearer <accessToken>
```

Chưa kết nối:
```json
{
  "workspaceId": "WS_ID",
  "status": "NOT_CONNECTED"
}
```

Đã kết nối (response **không** trả raw token):
```json
{
  "id": "OA_CONN_ID",
  "workspaceId": "WS_ID",
  "oaId": "1234567890123456789",
  "oaName": "Tên OA trên Zalo",
  "oaAvatarLight": null,
  "oaAvatarDark": null,
  "tokenExpiredAt": "2026-05-28T10:00:00.000Z",
  "status": "CONNECTED",
  "connectedAt": "2026-05-27T10:00:00.000Z",
  "createdAt": "...",
  "updatedAt": "...",
  "hasAccessToken": true,
  "hasRefreshToken": true
}
```

Các trạng thái FE cần xử lý UI:
- `NOT_CONNECTED` — hiện nút "Kết nối Zalo OA"
- `CONNECTED` — cho phép template/messaging
- `TOKEN_EXPIRED` — hiện nút "Kết nối lại" (gọi lại `POST connect`)
- `DISCONNECTED` — đã ngắt kết nối thủ công

### 6.4 Disconnect OA

```http
POST /workspaces/:workspaceId/oa/disconnect
Authorization: Bearer <accessToken>
```

Response:
```json
{ "ok": true }
```

Ghi chú audit log:
- `oa.connected` — ghi khi OAuth callback **thành công** (`GET /oa/auth/callback`).
- `oa.disconnected` — ghi khi `POST disconnect`.

## 7) Templates (CRUD + submit + staff approve/reject)

Base:
- `/workspaces/:workspaceId/templates`

### 7.1 Create template
Auth/Role: `OWNER` hoặc `MEMBER`

Endpoint:
- `POST /workspaces/:workspaceId/templates`

Request (không gửi `code`; backend tự sinh):
```json
{
  "name": "Template Demo",
  "content": "<zns>..</zns>",
  "placeholdersJson": { "name": "string" }
}
```

Ghi chú:
- `code` do backend gán khi tạo: chuỗi **6 chữ số**, random trong khoảng **100000–999999**, **unique trong workspace**. Frontend lấy `code` từ **response** của `POST` (cùng các field như list/get).

### 7.2 List templates
- `GET /workspaces/:workspaceId/templates`

### 7.3 Get template
- `GET /workspaces/:workspaceId/templates/:templateId`

### 7.4 Update template
- `PATCH /workspaces/:workspaceId/templates/:templateId`

Ghi chú:
- Backend chặn sửa nội dung khi template đã `APPROVED`, **trừ** `unitPricePerMessage` (chỉ `UserRole.ADMIN`).
- Field `code` chỉ được đổi khi JWT user có `UserRole` là `ADMIN` hoặc `STAFF`; `USER` gửi `code` sẽ nhận `403`.
- `unitPricePerMessage` (VND/tin): chỉ `ADMIN` set; nếu null thì dùng giá mặc định từ `GET /admin/config/message-pricing`.

### 7.5 Submit template
- `POST /workspaces/:workspaceId/templates/:templateId/submit`

Response hiện tại là cập nhật trạng thái sang `PENDING_ZALO_APPROVAL` (chờ staff duyệt và gắn `providerTemplateId` từ Zalo).

Ghi chú gửi ZNS:
- Sau khi staff **approve**, template phải có `providerTemplateId` (ID template trên Zalo Business) thì mới gửi tin được.
- `data` khi send message phải khớp `placeholdersJson` của template.

### 7.7 Campaigns (ZNS hàng loạt)

Auth/Role: `OWNER` hoặc `MEMBER` + workspace context.

- `POST /workspaces/:workspaceId/campaigns` — body `{ "name", "templateId" }` (template phải `APPROVED`)
- `GET /workspaces/:workspaceId/campaigns`
- `GET /workspaces/:workspaceId/campaigns/:campaignId`
- `PATCH /workspaces/:workspaceId/campaigns/:campaignId` — chỉ đổi `name` (không đổi template)
- `DELETE /workspaces/:workspaceId/campaigns/:campaignId` — `DRAFT`: xóa hẳn; đã chạy: `CANCELLED`
- `GET /workspaces/:workspaceId/campaigns/:campaignId/csv-template` — tải CSV mẫu (cột `Phone` + placeholder)
- `POST /workspaces/:workspaceId/campaigns/:campaignId/import-csv` — `multipart/form-data` field `file` (thay toàn bộ rows; chỉ trước lần execute đầu)
- `GET /workspaces/:workspaceId/campaigns/:campaignId/rows?status=&limit=`
- `PATCH /workspaces/:workspaceId/campaigns/:campaignId/rows/:rowId`
- `DELETE /workspaces/:workspaceId/campaigns/:campaignId/rows/:rowId`
- `POST /workspaces/:workspaceId/campaigns/:campaignId/execute` — gửi tuần tự, trừ ví OWNER trước, hoàn tiền tin failed
- `POST /workspaces/:workspaceId/campaigns/:campaignId/retry` — body `{ "rowIds": ["..."] }` (chỉ rows `FAILED`, trừ ví lại)

Dữ liệu từng dòng nằm ở bảng `CampaignRow` (`phoneNumber`, `payloadData` JSON, `status`, `messageLog`).

### 7.6 Staff review APIs (internal)
Base:
- `/internal/templates`
- `/internal/templates/:templateId`
- `/internal/templates/:templateId/mark-pending-zalo-approval`
- `/internal/templates/:templateId/approve`
- `/internal/templates/:templateId/reject`
- `/internal/templates/:templateId/disable`

Auth/Role: `STAFF` (UserRole.STAFF)

List queue:
```http
GET /internal/templates?status=SUBMITTED&keyword=demo&limit=20
```

List response (tối giản):
```json
[
  {
    "id": "TEMPLATE_ID",
    "name": "Template Demo",
    "code": "482391",
    "status": "SUBMITTED",
    "workspace": {
      "id": "WS_ID",
      "name": "Demo Workspace",
      "slug": "demo-workspace",
      "status": "ACTIVE"
    },
    "oaConnection": {
      "id": "OA_CONN_ID",
      "oaId": "OA_123",
      "oaName": "Demo OA",
      "status": "CONNECTED"
    }
  }
]
```

Detail:
```http
GET /internal/templates/:templateId
```

Mark pending zalo approval:
```http
POST /internal/templates/:templateId/mark-pending-zalo-approval
```

Approve request (**bắt buộc** `providerTemplateId` — ID template ZNS trên Zalo):
```json
{ "providerTemplateId": "ZALO_TPL_123" }
```

Ghi chú:
- `providerTemplateId` lấy từ Zalo OA Manager sau khi template được Zalo duyệt.
- Không có field này → API gửi tin trả `400 Template is missing Zalo providerTemplateId`.

Reject request:
```json
{ "reason": "Rejected for demo" }
```

Disable:
```http
POST /internal/templates/:templateId/disable
```

Sau khi approve xong, template trở thành `APPROVED` để FE có thể gửi message.

## 8) Messaging (send single + logs) — Zalo ZNS thật

Base:
- `/workspaces/:workspaceId/messages`

Backend gọi API Zalo `POST https://business.openapi.zalo.me/message/template`, tự refresh access token nếu sắp hết hạn.

**Điều kiện gửi thành công:**
- OA `CONNECTED`
- Template `APPROVED` và có `providerTemplateId`
- Ví workspace owner đủ số dư (hiện `1000` / tin)

### 8.1 Send single
Auth/Role: `OWNER` hoặc `MEMBER`

Endpoint:
- `POST /workspaces/:workspaceId/messages/send-single`

Request:
```json
{
  "templateId": "TEMPLATE_ID",
  "phoneNumber": "84901234567",
  "data": { "name": "Minh" }
}
```

Ghi chú:
- `phoneNumber`: format quốc tế không dấu `+` (ví dụ `84901234567`).
- `data`: object placeholder khớp template ZNS đã approve.

Response thành công:
```json
{
  "status": "SUCCESS",
  "providerMessageId": "MSG_ID_TU_ZALO",
  "messageLogId": "MESSAGE_LOG_ID"
}
```

Response lỗi (ví dụ Zalo từ chối / thiếu credit / thiếu providerTemplateId):
```json
{
  "statusCode": 400,
  "message": {
    "message": "Zalo ZNS send failed",
    "messageLogId": "MESSAGE_LOG_ID"
  },
  "error": "Bad Request"
}
```

Ghi chú nghiệp vụ:
- **Chỉ trừ ví** khi Zalo trả success.
- Nếu Zalo fail, backend vẫn tạo `MessageLog` với `status: FAILED` (có `messageLogId` trong body lỗi).

### 8.2 List message logs
Endpoint:
- `GET /workspaces/:workspaceId/messages/logs?status=SUCCESS&sendType=SINGLE&limit=50`

Query:
- `status` (MessageStatus)
- `sendType` (SendType)
- `limit` (optional)

## 9) Wallet & Topup (QR mock + webhook mock + trừ credit)

### 9.1 Get wallet balance
- `GET /workspaces/:workspaceId/wallet/balance`

Response:
```json
{
  "ownerUserId": "USER_ID",
  "balance": "1000",
  "totalTopup": "1000",
  "totalSpent": "0",
  "totalRefund": "0"
}
```

### 9.2 Wallet transactions
- `GET /workspaces/:workspaceId/wallet/transactions?type=MESSAGE_DEBIT`

### 9.3 Create topup QR (mock)
Auth/Role: `OWNER`

Endpoint:
- `POST /topups/workspaces/:workspaceId/qr`

Request:
```json
{
  "amountExclVat": 100000
}
```

Response:
```json
{
  "id": "TOPUP_ID",
  "topupCode": "TP_...",
  "paymentProvider": "mock_gateway",
  "paymentRef": "PR_...",
  "qrContent": "qr_TP_...",
  "qrExpiredAt": "..."
}
```

### 9.4 Payment webhook (admin, mock)
Endpoint:
- `POST /topups/webhook`

Auth/Role: UserRole.ADMIN

Request (success):
```json
{
  "provider": "mock_gateway",
  "eventId": "EVT_1",
  "topupCode": "TP_...",
  "status": "success",
  "amountInclVat": 110000
}
```

Response:
```json
{ "ok": true }
```

Ghi chú:
- Backend check idempotency theo `(provider, eventId)`.

## 10) API Key Management (owner)

Base:
- `/workspaces/:workspaceId/api-keys`

### 10.1 Create api key
Auth/Role: `OWNER`

Request:
```json
{
  "name": "Public API Key Demo",
  "keyPrefixHint": "PUB"
}
```

Response:
```json
{
  "apiKey": "RAW_API_KEY_ONCE",
  "id": "API_KEY_ID",
  "name": "Public API Key Demo",
  "keyPrefix": "PUB...",
  "status": "ACTIVE",
  "createdAt": "..."
}
```

### 10.2 Regenerate api key
Endpoint:
- `PATCH /workspaces/:workspaceId/api-keys/:apiKeyId/disable` (OWNER)
- `POST /workspaces/:workspaceId/api-keys/:apiKeyId/regenerate` (OWNER)

Response regenerate:
```json
{ "apiKey": "NEW_RAW_API_KEY" }
```

Ghi chú audit log:
- Backend tự ghi audit log khi `create` / `disable` / `regenerate` API key (không cần FE gọi endpoint riêng).

## 11) Audit Log

Backend **tự ghi** log sau API nghiệp vụ thành công. FE **chỉ đọc** qua endpoint read-only bên dưới (không có API ghi log).

### 11.1 Owner — xem log workspace của mình

- `GET /workspaces/:workspaceId/audit-logs`

Headers:
- `Authorization: Bearer <accessToken>`
- `x-workspace-id: <workspaceId>`

Auth/Role: `OWNER` (chỉ workspace mình sở hữu / là owner)

Query:
- `action` (optional)
- `limit` (optional, default `50`, max `200`)

### 11.2 Admin — xem toàn hệ thống hoặc lọc theo workspace

- `GET /admin/audit-logs?workspaceId=&action=&limit=50`

Auth/Role: `UserRole.ADMIN`

Query:
- `workspaceId` (optional) — bỏ trống = tất cả workspace; có giá trị = chỉ workspace đó
- `action`, `limit` — giống mục 11.1

### 11.3 Response mẫu (Owner & Admin)

```json
[
  {
    "id": "AUDIT_LOG_ID",
    "actorUserId": "USER_ID",
    "workspaceId": "WS_ID",
    "action": "member.invited",
    "resourceType": "WorkspaceInvitation",
    "resourceId": "INVITATION_ID",
    "metadataJson": { "inviteValue": "member@example.com" },
    "createdAt": "...",
    "actor": {
      "id": "USER_ID",
      "email": "owner@example.com",
      "fullName": "Owner Name"
    }
  }
]
```

### 11.4 Action được ghi tự động (không gọi API riêng)

| Action | API trigger | Role |
|--------|-------------|------|
| `workspace.created` | `POST /workspaces` | user tạo workspace |
| `workspace.updated` | `PATCH /workspaces/:workspaceId` (đổi name/slug) | OWNER |
| `billing_info.updated` | `PATCH /workspaces/:workspaceId` (đổi billing) | OWNER |
| `member.invited` | `POST /workspaces/:workspaceId/invitations` | OWNER |
| `member.removed` | `DELETE /workspaces/:workspaceId/members/:memberUserId` | OWNER |
| `oa.connected` | OAuth callback thành công `GET /oa/auth/callback` | Public (Zalo redirect) |
| `oa.disconnected` | `POST /workspaces/:workspaceId/oa/disconnect` | OWNER |
| `template.submitted` | `POST /workspaces/:workspaceId/templates/:templateId/submit` | OWNER hoặc MEMBER |
| `template.approved` | `POST /internal/templates/:templateId/approve` | STAFF |
| `template.rejected` | `POST /internal/templates/:templateId/reject` | STAFF (bắt buộc `reason`) |
| `api_key.created` | `POST /workspaces/:workspaceId/api-keys` | OWNER |
| `api_key.disabled` | `PATCH /workspaces/:workspaceId/api-keys/:apiKeyId/disable` | OWNER |
| `api_key.regenerated` | `POST /workspaces/:workspaceId/api-keys/:apiKeyId/regenerate` | OWNER |

## 12) Public API (gọi bằng API key)

Base:
- `/public/...`

### 12.1 Auth bằng `api_key`
- Backend Public API dùng `ApiKeyGuard` và hiện đọc:
  - header `x-api-key`
  - hoặc body/query `api_key`

Để tránh fail DTO cho endpoint `POST /public/messages/send`, bạn nên gửi cả:
- header `x-api-key: <api_key>`
- field `api_key` trong body.

### 12.2 List approved templates
- `GET /public/templates`

Response:
```json
[
  {
    "id": "TEMPLATE_ID",
    "name": "Template Demo",
    "code": "482391",
    "placeholdersJson": {},
    "status": "APPROVED"
  }
]
```

### 12.3 Get template detail
- `GET /public/templates/:templateId`

### 12.4 Send public message
- `POST /public/messages/send`

Request:
```json
{
  "api_key": "RAW_API_KEY",
  "templateId": "TEMPLATE_ID",
  "phoneNumber": "84901234567",
  "data": { "name": "Minh" }
}
```

Response (Zalo ZNS thật):
```json
{
  "status": "success",
  "providerMessageId": "MSG_ID_TU_ZALO",
  "messageLogId": "MESSAGE_LOG_ID"
}
```

Ghi chú:
- Cùng điều kiện như mục 8: OA connected, template approved + `providerTemplateId`, đủ credit.
- Lỗi gửi tin trả `400` kèm `messageLogId` nếu đã ghi log FAILED.

## 13) Admin APIs (nếu FE có màn admin)

Base:
- `/admin/...`

Auth/Role: `UserRole.ADMIN`

Endpoint ví dụ:
- `GET /admin/users`
- `PATCH /admin/users/:userId/lock` body `{ "locked": true }`
- `GET /admin/workspaces`
- `PATCH /admin/workspaces/:workspaceId/disable`
- `GET /admin/config/message-pricing` — giá mặc định VND/tin (mặc định 400)
- `PATCH /admin/config/message-pricing` body `{ "defaultMessageUnitPrice": 400 }`

## 14) Common error handling (frontend)

Backend NestJS thường trả lỗi theo dạng (tùy middleware/exceptions):
```json
{
  "statusCode": 401,
  "message": "Unauthorized",
  "error": "Unauthorized"
}
```

Frontend nên:
- bắt error từ `comviaFetch`
- hiển thị thông điệp `error.message`
- với lỗi gửi ZNS (`400`), `message` có thể là **object** `{ message, messageLogId }` — ưu tiên hiển thị `message.message` và link tới log nếu có `messageLogId`

## 15) Demo scenario gợi ý để test UI nhanh

Kịch bản 1 (user -> verify OTP -> workspace -> oa -> template -> staff approve -> send):
1. `POST /auth/register` (lấy `demoOtpCode`)
2. `POST /auth/otp/verify` (nhận `accessToken`)
3. `POST /workspaces` (OWNER)
4. `POST /workspaces/:wsId/oa/connect` → FE mở `authorizationUrl` (Zalo OAuth)
5. User cấp quyền → `GET /oa/auth/callback` → backend 302 về `https://app.comvia.cloud/app/w/:wsId/oa?status=success`
6. FE trang OA đọc query → `GET /workspaces/:wsId/oa/status` → `CONNECTED`
7. `POST /workspaces/:wsId/templates` (OWNER)
8. `POST /workspaces/:wsId/templates/:tplId/submit`
9. `POST /internal/templates/:tplId/approve` (STAFF) body `{ "providerTemplateId": "ZALO_TPL_ID" }`
10. Nạp credit ví (topup) nếu cần
11. `POST /workspaces/:wsId/messages/send-single`

Kịch bản 2 (API key -> public send):
1. Tạo api key (owner)
2. Đảm bảo OA connected + template approved có `providerTemplateId`
3. Gọi `POST /public/messages/send` với `x-api-key` + `api_key` trong body

## Phụ lục A) Enum / status (API response)

Backend trả enum **UPPER_SNAKE_CASE** (theo Prisma). FE nên map label hiển thị ở client, không đổi giá trị gửi/nhận.

| Nhóm | Giá trị |
|------|---------|
| `UserRole` | `USER`, `ADMIN`, `STAFF` |
| `UserStatus` | `ACTIVE`, `PENDING_VERIFICATION`, `LOCKED` |
| `WorkspaceStatus` | `ACTIVE`, `DISABLED`, `SUSPENDED`, `DELETED` |
| `MemberRole` | `OWNER`, `MEMBER` |
| `InvitationStatus` | `PENDING`, `ACCEPTED`, `EXPIRED`, `REVOKED`, `RESENT` |
| `BillingType` | `ORGANIZATION`, `INDIVIDUAL` |
| `OaConnectionStatus` | `NOT_CONNECTED`, `CONNECTED`, `TOKEN_EXPIRED`, `CONNECTION_ERROR`, `RECONNECT_REQUIRED`, `DISCONNECTED` |
| `TemplateStatus` | `DRAFT`, `SUBMITTED`, `PENDING_ZALO_APPROVAL`, `APPROVED`, `REJECTED`, `DISABLED` |
| `TopupStatus` | `PENDING`, `PAID`, `FAILED`, `EXPIRED` |
| `OrderStatus` | `PAID`, `CANCELLED` |
| `InvoiceStatus` | `POSTED`, `ISSUED`, `CANCELLED` |
| `WalletTransactionType` | `TOPUP_CREDIT`, `MESSAGE_DEBIT`, `CAMPAIGN_HOLD`, `CAMPAIGN_REFUND`, `MANUAL_ADJUSTMENT`, `REVERSAL` |
| `SendType` | `TEST`, `SINGLE`, `CAMPAIGN` |
| `MessageStatus` | `PENDING`, `SUCCESS`, `FAILED` |
| `CampaignStatus` | `DRAFT`, `READY`, `RUNNING`, `PAUSED`, `COMPLETED`, `FAILED`, `CANCELLED` |
| `CampaignRowStatus` | `PENDING`, `SUCCESS`, `FAILED`, `SKIPPED` |
| `ApiKeyStatus` | `ACTIVE`, `DISABLED`, `REVOKED` |

Nguồn sự thật: `prisma/schema.prisma`.

